# Validator Integration

The v0.1 validator recursively discovers schemas and examples, self-validates Draft 2020-12 schemas, rejects duplicate canonical `$id` values, and maps stable v0.1 and draft-v0.2 filenames independently. Global format checking remains unchanged; behavioral record timestamps receive explicit semantic validation.
