# D.0 Test Plan

Validate every draft schema recursively, validate all examples, assert unique schema IDs, test stable v0.1 compatibility, typed references, semantic choice/trial/record constraints, and constructor self-validation. Node runtime and compiler regressions remain outside D.0 scope.
